# --- PyTorch + Flash Attention（三者须一致，否则 flash-attn 无法安装或运行报错）---
# 说明：
# - 预编译 flash-attn 的 whl 仅支持 Linux x86_64；Windows 请用 WSL2 或源码编译。
# - 文件名里的「cu12torch2.2」表示 CUDA 12.x + PyTorch 2.2.x；请用下方 cu121 的 PyTorch 官方包与之对齐。
# - 若遇 undefined symbol / ABI 报错，把 URL 中 cxx11abiFALSE 换成同版本 cxx11abiTRUE 再试。
# - Python 版本：把 URL 里的 cp311-cp311 改成你的版本（如 cp310-cp310、cp312-cp312）。
# - 若必须用 RTX 5090 的 cu128 PyTorch，官方可能没有完全匹配的预编译 flash wheel，需源码安装 flash-attn（见项目文档）。
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.2.2
torchvision==0.17.2

# 与 torch==2.2.x + cu12 预编译包对应（Dao-AILab v2.7.3）
flash-attn @ https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.3/flash_attn-2.7.3+cu12torch2.2cxx11abiFALSE-cp311-cp311-linux_x86_64.whl

# Core training dependencies
numpy==1.26.4
matplotlib>=3.3.0
pandas>=1.1.4
pillow>=7.1.2
requests>=2.23.0
tqdm>=4.64.0
seaborn>=0.11.0
ultralytics-thop>=2.0.0

# Model / vision
timm==1.0.14
albumentations==2.0.4
opencv-python==4.9.0.80
scipy==1.13.0
pycocotools==2.0.7
supervision==0.22.0
PyYAML==6.0.1
safetensors==0.4.3
huggingface-hub==0.23.2

# ONNX export
onnx>=1.14.0
onnxslim>=0.1.31
# onnxruntime-gpu replaces onnxruntime (CPU variant); supports CUDA 12.x
onnxruntime-gpu>=1.19.0

# System / profiling
psutil==5.9.8
py-cpuinfo==9.0.0

# Demo / serving (optional) 
gradio==4.44.1
